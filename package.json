{
    "name": "devops-streak-play",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "test": "pnpm -r test",
        "lint": "pnpm -r lint",
        "build": "pnpm -r build",
        "dev": "pnpm -r dev"
    },
    "devDependencies": {
        "husky": "^9.0.0",
        "prettier": "^3.0.0"
    },
    "engines": {
        "node": ">=20",
        "pnpm": ">=8"
    }
}